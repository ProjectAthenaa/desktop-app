import React, {useEffect} from 'react';
import './styles.scss';

type Props = {
}

const AuthTemplate: React.FC<Props> = () => {

  return (
    <div className={'auth'}>
     <h1>Auth</h1>
    </div>
  );
};

export default AuthTemplate;
